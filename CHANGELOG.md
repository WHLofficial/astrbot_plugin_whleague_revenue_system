# Changelog

本文件记录本项目的显著变更，格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [2.2.0] - 2026-08-23

### Added

- **`/主场事件取消 <队名> [事件名|事件id]`（管理）**：取消本窗口分配给球队的事件，误分配可撤。
  - **选择型**：未定/已选未结 → 删除待定行与广播记录（触发时未动账，无需回退），可重新触发；已结算拒绝并引导 `/主场结算 强制` 重算。
  - **即发型**：按该实例日志的 effect_json 回退生效数值——删对应事件流水并重算余额（资金/维护精确回退）、死忠按原比例反漴（乘法可交换，多事件叠加同样正确；触及 0/上限钳制有微小误差）、上座修正未随赛果消耗才回退（否则跳过并提示）。
  - **同名事件多次触发**：每次取消最新一条实例，回复注明剩余条数。
  - 只带队名则列出该队本窗口全部事件（类型/状态/id）；事件引用支持中文名或事件 id。
  - **事件重名防护**：`/主场事件写` 与草稿采纳拒绝与**已采纳**事件同名（流水备注与选择录入均按名匹配，重名会歧义/误删；pending 草稿间不算冲突）。
  - 无 schema 变更（回退依据复用既有 events_log.effect_json / event_choices / 流水表）。

## [2.1.0] - 2026-08-22

### Added

- **可命名赛季**：新表 `season_names`（schema v9，每赛季一条展示标签；赛季身份仍是整数序号，查询/过滤不受影响）。
  - **`/主场赛季命名 <名称>`**（管理）：给当前赛季命名/反复改名，规则同 `/主场改名`（非空、≤30 字、无换行；与现名相同提示「未变化」）。
  - **`/主场推进 赛季 [名称]`**：推进时可直接给新赛季命名；名称在推进前校验，非法则整体不推进。
  - **展示「有名只显名」**：命名后消息文案（导入/推进/事件选择/事件结算/窗口结算/主场卡片/轮次统计/财务）与三张图表标题均显示赛季名，未命名回退「第 N 赛季」；图表文件名仍用序号，历史赛季名保留。

## [2.0.0] - 2026-08-22

### Added

- **`/主场帮助` 分级帮助命令**：按发者权限显示玩家段，管理员追加管理段（对标成长插件 `/成长`）；帮助文案集中在 `utils/help_text.py`，与 README 命令清单同步维护。
- **`/主场天气覆盖 <轮次> <主队> <天气>`**：天气写入从查询命令中独立成命令，读写分离。

### Changed

- **命令体系全量规范化（40 → 38 个，全部统一「主场」前缀）**：
  - `主场赛果` → `主场赛果录入`（名字曾是查询样、实为录入）；`球场活动` → `主场档期`（与「主场事件」区分）；`冠名`/`退冠名` → `主场冠名`/`主场退冠名`（补前缀）。
  - **合并四组**：`主场`+`主场信息` → `主场 [队名]`（无参=自己，带参=任意队）；`主场推进窗口`+`主场推进赛季` → `主场推进 <窗口|赛季>`（枚举参数）；`主场属性`（单队）并入 `主场属性导入`（每行「队名 影响力 容量 等级」，单行即单队，`-` 不改）；`主场事件选择导入`（批量）并入 `主场事件选择`（单行=单条、多行=批量，行格式一致）。
  - **`主场天气` 拆为纯查询**：带 4 参时提示改用 `/主场天气覆盖`，不再读写二合一。
  - **`主场轮次统计`（文本版）管理 → 全员可查**：与图版（本就全员）权限对齐，消除「只差一个图字、权限却不同级」的割裂。

### Deprecated

- **旧命令名以别名（alias）过渡一版，v3.0 移除**：`主场赛果`、`主场推进窗口`、`主场推进赛季`、`主场事件选择导入`、`主场信息`、`球场活动`、`冠名`、`退冠名`（README 命令清单附旧名 → 新用法对照表）。

## [1.6.0] - 2026-08-22

### Added

- **管理员代改球场名 `/主场改名 <队名> <新名>`**：管理员免费为任意球队改球场名（不记流水、不占用任何额度）；该队还没有球场时报错、不自动建场，防止手滑队名凭空建场；新名需非空、≤30 字、不含换行，可含空格（取队名后的整段），与现名相同则提示「名称未变化」。

### Removed

- **移除玩家 `/球场命名` 命令**：改名收归管理员统一操作。随之清理 `rename_fee` 配置（defaults/配置面板）、`mark_rename_used` DAO 方法与 `stadium.free_rename_used` 列（新建库不再创建该列；存量库留列无害、无需迁移，代码不再引用）。

## [1.5.0] - 2026-08-19

### Added

- **文件导入改为本地 `imports/` 目录 + 群文件钩子自动导入（对标 WHL 成长/谈判插件）**：文件导入不再依赖「聊天里附带文件」，改为数据库同目录的固定 `imports/` 目录。两种通道：①群里直接发文件即自动导入——按**文件名前缀判型**（`赛程_`/`schedule_` → 赛程；`赛果_`/`result_`/`score_` → 赛果；`属性_`/`attr_`/`attribute_` → 属性），非命中文件名静默忽略；**赛果的轮次取自文件名前缀后剩余**（`赛果_顶级1.csv` → 轮次「顶级1」，契合轮次文本即身份），轮次未导入赛程时报错；②把文件放进 `imports/` 目录后用命令按文件名导入（`/主场赛程导入 赛程_第一周.csv`、`/主场赛果 <轮次> 赛果_xxx.csv`、`/主场属性导入 属性_xxx.csv`），命令不带文件名时列出目录现有文件。文件按 `check_import_file` 校验（防目录穿越、`.csv/.xlsx` 白名单、缺失列出目录文件、大小上限复用 `import_max_file_size_mb`）；群文件由 `on_group_file` 钩子（管理员 + 群白名单）接收，落盘 imports 目录后自动导入并回复结果。

### Fixed

- **独立事件结算回复展示 LLM 结果短文案**：`/主场事件结算 [全部]` 此前直接罗列数据变化（如「资金 +2.5M；维护 −1.0M」），LLM 生成的结果播报只见于窗口结算的「事件·」回顾、结算回复里看不到；现结算回复优先显示 LLM 生成的结果短文案（已含效果说明），LLM 无内容/未启用时回退确定性数据变化行，与窗口结算一致。

## [1.4.0] - 2026-08-19

### Added

- **图表字重与清晰度**：标题/表头/合计行在仅内置字体（含云服务器/容器）环境下也真正渲染为粗体——内置 Noto Sans SC 变量字体按字重选择 Bold/Regular，不再全部压在 Regular；全部图表改用 2x 超采样绘制后 LANCZOS 缩回设计尺寸，文字与线条边缘更锐利、不再发虚。
- **赛程导入不限制轮次有无数字——轮次即文本、同赛事按导入顺序定序**：轮次列以**文本为身份**、不再被解读成轮次号——前缀识别赛事（顶级→顶级联赛…，无前缀→联赛），**同名文本即同一轮**；同一赛季内每个**新出现的轮次文本**按导入顺序得 1,2,3… 的内部轮次号，**赛季推进后各赛事归一、从 1 重新编号**（数据库结构 v8，`round_names` 按（赛季,赛事,文本）唯一）。天气/赛果/统计/图表命令一律用轮次文本引用（如 `顶级9` / `小组赛`），未导入的文本报「尚未导入」；`顶级9` 不再固定等于「第 9 轮」，按该赛事首次出现顺序编号（排表按 1..18 顺序导入时仍为 9）。
- **赛果支持按比分自动判胜负**：文本与文件导入赛果时可直接写「主队 2-1」「主队 0-0PK2-4」等比分，自动推导主队胜/平/负并保留比分原文（常规平且有点球时按点球判；支持连字符/半角/全角冒号）；显式 胜/平/负 仍可用、仍优先。
- **重复对阵导入不再误跳过**：去重粒度由「同轮同赛事同主队」收窄为「同轮同赛事同完整对阵（主队+客队）」——同一窗口内主队多场（重复对阵/双回合）照常导入；完全相同的行重复导入仍跳过并提示（数据库结构 v7）。
- **预告图排版修复**：轮次预告图画布高度改为按实际绘制范围计算，去掉底部多余空白；表格单元格文字超宽时自动缩小字号（下限 11px），仍超宽则截断加省略号，超长队名/球场名不再与相邻列文字重叠（轮次统计图同享防溢出）。
- **赛果支持「比赛取消」**：文本与文件导入可写「主队 取消 / C / cancel」——取消后无观众、无收益、不记任何流水；轮次上座图该行观众/上座率显示 `-`、不计入合计与场均（全取消时场均=0）；赛季走势图按 0 标记；轮次统计文本记为「比赛取消（不计入合计）」；状态积分（近 3 场 Pts）忽略取消场次、取消不占名额，上座走势从已赛各场往前补足 3 场。

### Fixed

- **修复轮次预告图「天气预报」区块消失的回归**：2x 超采样改造后第二个区块的起绘 y 被错误地二次缩放，天气块被画到画布外不可见；现统一 `_draw_table_block` 的 y 坐标系（传入即设备坐标），预告图恢复为「对阵表 + 天气预报」两区块。新增回归测试（统计表头色带数 ≥2）。
- **命名轮次命令侧改为只读**：天气/赛果/统计/图表命令输入轮次文本时从登记表查询，未导入的直接提示「尚未导入」，不再自动创建登记记录（拼错不再留下幽灵记录）；登记只由导入侧建立。
- **matches/round_names 表重建迁移原子化（schema v2/v7/v8）**：建表重建改为显式事务包裹，任一语句失败即回滚——保留旧表与数据、版本号不推进，下次启动可重试，避免中断后残留空表导致赛程静默丢失。
- 轮次上座图兜底：已置赛果但缺观众（异常数据）的行按未录入跳过，不再 `int(None)` 崩溃。
- `/主场赛果` 反馈结果中文化（胜/平/负/取消，与图表一致），不再显示 W/D/L 字母。

## [1.3.0] - 2026-08-18

### Added

- **事件广播叙述化**：选择型事件广播改为「LLM 叙述段 + 选项列表（**不显示概率**）」——管理员可整段转发给教练选择，概率只留到结算时掷骰。
- **独立事件结算指令** `/主场事件结算 [全部]`：导入选择后可提前结算，不必等窗口结算（默认只结已导入选择；加「全部」连未定的一并按最差兜底；流水为普通 event 流水，窗口强制重算不会误删）。
- **结算结果短文案**：每个已结选择事件由 LLM 生成 1~2 句结果播报并写入结算回顾（失败回退确定性摘要）。
- **图表字体通用化**：插件内置 Noto Sans SC（OFL），字体按「`chart_font_path` 覆盖 → 跨平台已知名单（微软雅黑等）→ 内置 fonts/ → 系统目录扫描」四级兜底，云服务器/容器无需另装中文字体；内置变量字体自动取 Regular 字重。

### Fixed

- **修复 LLM 空内容根因（适配新版 AstrBot `text_chat` 返回）**：v4.2x 的 `provider.text_chat` 返回 `LLMResponse`（文本在 `completion_text`），旧代码只读 `result_str` 恒为空；现按 `completion_text` → `result_str`（旧版）→ `result_chain.get_plain_text()` → 纯字符串的顺序容错提取。
- **LLM 空内容可诊断与自愈**：`_ask` 按失败原因分类告警（provider 未选用 / 缺 `result_str` / 空内容 / 超时），事件与品牌设计空内容静默**重试一次**后再报错（错误含可操作提示）；文案回退模板时也写日志。
- 修复 LLM 事件设计 prompt 的 `{{team}}` 双花括号缺陷：模板占位符现为单花括号，可被 `_fill_template` 正确替换。
- 默认 LLM 超时 15s → 60s（`llm_timeout_seconds`，长 prompt 的事件设计不再容易中途掐断）；超时捕获兼容 Python 3.10（`asyncio.TimeoutError` vs 内置 `TimeoutError`）。
- 事件选项号解析（1~4 / ①②③④）收敛到 `utils/security.parse_choice_no`，服务端与管理命令统一引用。
- **修复管理命令成功路径崩溃**：`/主场事件采纳/丢弃`、`/主场事件写`、`/主场品牌采纳/丢弃` 等服务成功返回 `None` 时，handler 的 `if "error" in result` 抛 `TypeError`；`_run` 现仅对 `None` 归一为空 dict（list 返回如选择列表/批量导入原样保留）。
- **强制重算（`/主场结算 强制`）选择事件只重记账目流水**，跳过死忠/上座等持续状态类效果，避免百分比复利叠加（与死忠演化「重算不重复状态」一致）。
- `/主场事件选择` 支持 ①②③④ 符号录入（与 `/主场事件选择导入` 一致）。
- `/主场事件列表` 恢复效果信息：即发显示 `effects_json`、选择显示选项数。
- 选择事件广播概率显示去尾零（`60.0%` → `60%`）。

## [1.2.0] - 2026-08-18

### Added

- 新增「选择型随机事件」：触发只广播选项、进入待定，玩家在 2~4 个操作中任选其一，每项操作带**正面/负面概率表**（内置 16 条选择型事件，覆盖既有 8 类）。
- 管理员新命令：`/主场事件选择 <队名> <事件名> <选项号>`（单条录入）、`/主场事件选择导入`（多行批量，支持 ①②③④）、`/主场事件选择列表 [窗口]`（待定/已选/已结算状态）。
- 结算内置选择事件兑现：球员已选选项按该选项概率表掷骰执行；未收到回应自动按该事件**最差结果**（净额 = 资金 − 维护，取最小）执行。
- 数据库结构 v5：`event_pool` 新增 `event_type`/`options_json` 列，并新建 `event_choices` 玩家决策表；v4→v5 自动迁移（旧事件默认即发型）。
- `/主场事件 <队名> [事件id]`：单队触发支持强制指定事件。
- LLM 事件设计可产出选择型草稿；新增选择事件结构校验（2~4 个选项、每选项 ≥2 条概率结果并归一、每项保证一条正面与一条负面路径）与逐效果钳制。
- `/主场事件写 <名称> <类别> <权重> choice <选项JSON>`：管理员可手写选择型事件。

### Changed

- 内置事件库改写为 **6 即发 + 16 选择**：即发事件（暴雨、TIFO 出圈、负面报道、文物、补贴、安保存漏洞）触发即生效；选择型事件由「触发即记账」改为「触发进待定、结算统一兑现」。
- `/主场事件`（全员分配）与 `/主场事件 <队名>` 输出即发结果与选择型广播文案（含选项与概率表）。
- 窗口结算（`/主场结算`）增加选择事件兑现环节，结算概览展示兑现摘要；强制重算会重置该窗口选择事件并随本次结算重新兑现。
- `/主场事件列表` 显示事件类型（即发/选择）。

### Fixed

- 修复 LLM 事件设计模板为空时的 `NameError` 兜底问题。

## [1.1.0] - 2026-08-17

### Added

- CSV/XLSX 文件批量导入赛程/赛果/属性：群聊附件入口，表头关键字自动识别列序，临时文件解析后自动清理。
- 赛事区分：轮次文字前缀识别（顶级/次级/冠军杯），同轮次同窗口多赛事共存互不干扰；图表按赛事配色（次级紫 / 顶级橙 / 其余绿）。
- 赛程/赛果补充字段：周（W12）、天（D6）、星期（由天生成单字 一~日）、开球时间、比分（数据库结构 v3）。
- 轮次上座统计图：11 列对齐「展示」工作表（周/天/星期/时间/天气/主队/比分/客队/球场/观众/上座率），含合计与场均，按赛事各出一张图。
- 赛季走势图：各队本赛季主场观众折线图（图例按场均上座排序）。
- 轮次预告图：上半对阵表 + 下半天气预报合成一张图（天气着色，待预报灰色）。
- 图表面向全体群友开放，生成图片存于 `charts/` 目录（超过 50 张自动清理最旧）。

### Fixed

- 强制结算重算精确撤销：按 `window_summaries.tx_ids` 记录删除上次结算创建的流水并重算余额，不再误删门票/事件等非结算流水。
- 死忠演化每窗口仅执行一轮；强制重算不重复演化、不重复扣冠名窗口数。
- LLM 事件草稿 ID 防覆盖（改用 MAX(id) 递增）；负维护钳制为 0（不允许退款）；`add_admin` 非法 QQ 参数校验；重复赛程导入计数修正。

## [1.0.0] - 2026-08-17

### Added

- 核心工作流：赛程导入 → 天气预报 → 赛果录入 → 上座/票房/商业/转播自动计算与记账 → 轮次/赛季统计。
- 球场属性管理（管理员）：影响力/容量/等级导入、越界校验、自动按定价记账；扩建/升级 25% 以建设券返还（仅可抵扣后续建设支出）。
- 子设施系统（商业区/灯光转播/草皮/青训中心/医疗中心，0~5 级）。
- 死忠球迷演化（非对称：涨粉看热度、掉粉看空场，叠加战绩修正与球迷开放日加成）。
- 档期活动（演唱会/电竞/球迷开放日/青训夏令营）与窗口结算（维护费 + 档期兑现 + 冠名费 + 死忠演化 → 账本 → 窗口摘要）。
- 冠名权：品牌池（内置/自定义/LLM 草稿）、签约/退约（提前解约赔偿、死忠流失时品牌有概率主动解约）、球场显示名联动。
- 随机事件（即发型）：全员/单队触发、条件过滤、LLM 文案与设计（破坏经济的数值会被钳制）。
- 谈判系统只读联动：球队列表、QQ↔球队绑定；谈判库不可用时自动回退纯管理员模式。
- 权限与安全：管理员白名单、群白名单、输入净化、频率限制。
- 配置（WebUI / `/主场设置`）与每日自动备份；零依赖测试基建（桩 + 临时库 + LLM 桩）。

[2.2.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/d6cc0ac...67dd59d
[2.1.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/33d41c6...e6c8bce
[2.0.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/6b8ccd7...a1447db
[1.6.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/147c777...bd33b8d
[1.3.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/a13c722...a98a161
[1.4.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/a98a161...443dd79
[1.5.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/38b26a9...4df6983
[1.2.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/f33cc93...a13c722
[1.1.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/3bd5d61...f33cc93
[1.0.0]: https://github.com/WHLofficial/astrbot_plugin_whleague_revenue_system/compare/c2e2cf7...3bd5d61
